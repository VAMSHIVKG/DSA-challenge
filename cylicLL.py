# create a cyclic LL 
class Node:
    def __init__(self, val=None):
        self.val=val
        self.next=None

# class LinkedList: 
#     def __init__(self): 
#         self.head = None

def showcycle(self):
    i=0
    t=self.head
    while i<100:
        print(t.val,end=',')
        t=t.next
        i+=1

a=Node(10)
b=a.next
# obj=LinkedList()
# obj.head=Node(10)
# obj.head.next=Node(20)
# obj.head.next.next=Node(30)
# obj.head.next.next.next=Node(40)
# obj.head.next.next.next=obj.head.next
# obj.show(head)

#checking the cyclic condition

# def cyclic(self)

#reorder of cyclic

    
